services:
  - type: web
    name: google-to-notion
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m streamlit run streamlit_ui.py --server.port $PORT --server.address 0.0.0.0
